import React from 'react';
import Layout from './layout';

const ViewPage: React.FC = () => {
    return (
        <Layout>
            <h1>View Page</h1>
            <p>This is a new page for viewing content.</p>
        </Layout>
    );
};

export default ViewPage;
